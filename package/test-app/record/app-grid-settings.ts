import { RevSimpleGridSettings } from '../..';
import { AppOnlyGridSettings } from './app-only-grid-settings';

/** @public */
export interface AppGridSettings extends AppOnlyGridSettings, RevSimpleGridSettings {

}
